export enum UserRoles {
    DRAFT =  'draft',
    ADMIN = 'admin',
    STAFF = 'staff',
    CUSTOMER = 'customer',
    DELETED = 'deleted',
 }

export enum OrderStaus {
    PAID = 'paid',
    UNPAID = 'unpaid',
    CANCELED = 'canceled',
}